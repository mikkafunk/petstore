package funk.shane.pets.domain;

/**
 * Only here as placeholder while I scope out the application framework
 */
public class PlaceHolder {
    public int addMe(int x, int y) {
        return x + y;
    }
}
